<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="view_calendar_event_form_inherit_vehicle" model="ir.ui.view">
        <field name="name">calendar.event.form.inherit.vehicle</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='location']" position="after">
                <field name="vehicle_id" string="Assigned Vehicle" options="{'no_create': True}"/>
            </xpath>
        </field>
    </record>

    <record id="view_calendar_event_calendar_inherit_vehicle" model="ir.ui.view">
        <field name="name">calendar.event.calendar.inherit.vehicle</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_calendar"/>
        <field name="arch" type="xml">
            <xpath expr="//calendar" position="inside">
                <field name="vehicle_id" string="Assigned Vehicle"/>
            </xpath>
        </field>
    </record>

</odoo>